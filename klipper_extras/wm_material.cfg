# Installed next to printer.cfg by tools/deploy_material.py; picked up by the
# `[include wm_material*.cfg]` line in printer.cfg. Remove this file (or run
# `deploy_material.py uninstall`) and macros fall back to temperature-based
# material inference.
[wm_material]
ini_path: ~/printer_data/config/tmt1.ini
